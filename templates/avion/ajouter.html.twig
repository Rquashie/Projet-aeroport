{% extends 'base.html.twig' %}

{% block content %}
    <h1> Ajouter un avion</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type = "submit">Ajouter</button>
    <a href=" ">Déja inscrit ? </a>
    {{ form_end(form) }}
{% endblock %}
