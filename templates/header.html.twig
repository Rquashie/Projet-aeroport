

<nav class="navbar navbar-expand-lg navbar-dark ftco_navbar bg-dark ftco-navbar-light" id="ftco-navbar">

    <div class="container">
        <a class="navbar-brand" href="{{ path('app_index') }}">Pacific<span>Travel Agency</span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#ftco-nav" aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="oi oi-menu"></span> Menu
        </button>

        <div class="collapse navbar-collapse" id="ftco-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active"><a href="index.html.twig" class="nav-link">Home</a></li>

                <li class="nav-item"><a  href="{{ path('app_user_index') }}" class="nav-link">Utilisateurs</a></li>
                {% if show_modal is defined and show_modal %}
                <div id="modal-user" class="modal" style="display: none">
                    <div class="modal-contenu">
                        <p>Espace réservé aux admins</p>

                        <a href=" {{ path("app_index")}}" class="fermer" >Fermer</a>
                    </div>
                </div>
                {% endif %}
                <<script>

                    window.addEventListener('DOMContentLoaded', function () {
                        document.getElementById('modal-user').style.display = 'block';
                    });
                </script>
                <style>
                    #modal-user {
                        display: none;
                        position: fixed;
                        z-index: 1;
                        left: 0;
                        top: 0;
                        width: 100%;
                        height: 100%;
                        overflow: auto;
                        background-color: rgb(0,0,0);
                        background-color: rgba(0,0,0,0.4);
                    }
                    .modal-contenu {
                        position: relative;
                        background-color: white;
                        margin: auto;
                        padding: 0;
                        border: 1px solid #888;
                        width: 20% ;
                        height: 10%;
                        box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2),0 6px 20px 0 rgba(0,0,0,0.19);
                        animation-name: animatetop;
                        animation-duration: 0.4s
                    }

                    .fermer {
                        display: inline-block;
                        margin-top: 15px;
                        background-color: #007BFF;
                        color: #fff;
                        border-radius: 4px;
                        text-decoration: none;
                    }
                </style>



                <li class="nav-item"><a href="{{ path('app_vol_index') }}" class="nav-link">Destinations</a></li>
                <li class="nav-item"><a href="{{ path('app_reservation_new') }}" class="nav-link">Réservations</a></li>
                <li class="nav-item"><a href="{{ path('app_avion_index') }}" class="nav-link">Avions</a></li>
                <li class="nav-item"><a href="{{ path('app_login') }}" class="nav-link">Connexion</a></li>
                <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link">Deconnexion</a></li>
                <li class="nav-item"><a href="contact.html" class="nav-link">Contact</a></li>
            </ul>
        </div>

    </div>


</nav>

